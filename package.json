{
  "name": "standup-therapy-deja-de-joder-2026",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "node scripts/build.js",
    "test": "node --test tests/*.test.js",
    "check": "npm test && npm run build"
  },
  "engines": {
    "node": ">=20"
  }
}
